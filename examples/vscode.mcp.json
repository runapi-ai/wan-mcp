{
  "servers": {
    "wan": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/wan-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
